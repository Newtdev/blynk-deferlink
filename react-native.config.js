// No native code ships in this package; the optional Install Referrer native
// module is provided by the app's own dependency when present.
module.exports = {
  dependency: {
    platforms: {
      android: null,
      ios: null,
    },
  },
};
